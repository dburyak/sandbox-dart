import 'dart:convert';

// Map<String, String> getMap(String valueStr) {
//   return json.decode(valueStr)
//       .map<String, String>((k, v) => MapEntry(k?.toString(), v?.toString()));
// }

void main() {
  // var valueStr = '{"first_name" : "fname","last_name" : "lname","gender" : 123}';
  //
  // Map<String, String> map = getMap(valueStr);
  // map.forEach((key, value) => print('$key $value'));
  // print(valueStr);
  //
  // var testMap = {
  //   'first_name': 'fname',
  //   'last_name': 'lname',
  // };

  print(Duration(seconds: 30));
}
